# Documentation cartes

- **`TILED_MAP_GUIDE.md`** — guide historique pour une intégration **Tiled + Phaser**. Le jeu utilise aujourd’hui des **grilles ASCII** générées (`server/models/maps/`). Conserver ce fichier comme **référence** si tu réintroduis un pipeline tuilé.

Les previews servies en jeu sont dans **`assets/served/map/`** (URL `/map/`).
